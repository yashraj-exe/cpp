#include<bits/stdc++.h>
using namespace std;
int main(){
    
    // vector<pair<int,int>> v = {{1,2},{2,3},{3,4}};
    
    // // vector pair ietatror
    // vector<pair<int,int>> :: iterator it;
    // for(it = v.begin(); it != v.end();it++){
    //     cout<<(*it).first<<" "<<(*it).second<<endl;
    // }

    //////////////////////////////////////////////////////////
     // second way 
     // when itertor point to a any pair then we can use -> syntax
    // for(it = v.begin(); it != v.end();it++){
    //     cout<<(it->first)<<" "<<(it->second)<<endl;
    // }
    
    
    return 0;
}