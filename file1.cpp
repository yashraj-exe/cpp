#include<iostream>
#include<fstream>
#include<string>
using namespace std;

int main(){
    // reading a file
    // string s;
    // ifstream read("t.txt");
    // getline(read,s); // read entire and save in s name variable 
    // cout<<s; 
    // in.close() not mendatory for close but is good prac for closing the streams

    // if i want to read alll data of afile then iwant use e0f() function
    // string s;
    // ifstream read("t.txt");
    
    // while(read.eof() == 0){
    //     getline(read,s);
    //     cout<<s<<endl;
    // }


    // wrirtting a file/
    // string s;
    // getline(cin,s);
    // ofstream write("t.txt");
    // write<<s;
    // write.close() not mendatory for close but is good prac for closing the streams


    return 0;
}